
CONFIG_GPIO=y
# Enable the Shell and the UART console
CONFIG_SHELL=y
CONFIG_UART_CONSOLE=y
# (Optional) Change the shell prompt from "uart:~$" to something custom
CONFIG_SHELL_PROMPT_UART="blackpill:~$ "

# Enable PWM
CONFIG_PWM=y